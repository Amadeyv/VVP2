#include <stdio.h>
#include <math.h>

int main() {
    int a, b, n, k;
    printf("a<b, a>0, b>0\n");
    printf("vvedite chisla a = ");
    scanf("%d", &a);
    printf("b = ");
    scanf("%d", &b);
    n = 0;
    k = a + 1;
    while (k<b) {
        while (n<k) {
            n = n + 1;
            printf("%d ", k);
        }
        k = k + 1;
        n = 0;
    }
    return 0;
}
