#include <stdio.h>
#include <math.h>

int main() {
    int n, v, r, v1, v2, k;
    printf("v massive net odinakovih chisel\n");
    printf("vvedite n = ");
    scanf("%d", &n);
    int arr[n];
    v = 0;
    while (v<n) {
        printf("arr[%d] = ", v);
        scanf("%d", &arr[v]);
        v = v + 1;
    }
    v1 = 1;
    v2 = 2;
    r = arr[1] - arr[0];
    while (v2<n) {
        k = arr[v2] - arr[v1];
        if (k==r) {
            v1++;
            v2++;
        }
        else {
            r = 0;
            break;
        }
    }
    printf("r = %d", r);
    return 0;
}
