#include <stdio.h>
#include <math.h>

int main() {
    int n, v, a, b, p;
    printf("vvedite n = ");
    scanf("%d", &n);
    printf("vvedite pervii chlen a = ");
    scanf("%d", &a);
    printf("vvedite vtoroi chlen b = ");
    scanf("%d", &b);
    int arr[n];
    v = 2;
    arr[0] = a;
    arr[1] = b;
    while (v<n) {
        p = v - 1;
        arr[v] = arr[0];
        while (p>0) {
        arr[v] = arr[v] + arr[p];
        p = p - 1;
        }
        printf("arr[%d] = %d\n", v, arr[v]);
        v = v + 1;
    }
    return 0;
}
