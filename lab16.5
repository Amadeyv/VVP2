#include <stdio.h>
#include <math.h>

int main() {
    int n, v, k;
    printf("vvedite n = ");
    scanf("%d", &n);
    int arr[n];
    v = 0;
    while (v<n) {
        printf("arr[%d] = ", v);
        scanf("%d", &arr[v]);
        v = v + 1;
    }
    v = 1;
    while (v<n) {
        printf("nechet arr[%d] = %d\n", v, arr[v]);
        v = v + 2;
    }
    if (n==((n/2)*2)) 
        v = n - 2;
    else 
        v = n - 1;
    while (v>-1) {
        printf("chet arr[%d] = %d\n", v, arr[v]);
        v = v - 2;
    }
    return 0;
}
