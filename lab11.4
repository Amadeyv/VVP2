#include <stdio.h>

int main() {
    int x, y, k;
    printf("vvedite koordinati x = ");
    scanf("%d", &x);
    printf("y = ");
    scanf("%d", &y);
    if ((x>0||x<0)&&(y>0||y<0)) {    
        if (x>0) {
            if (y>0)
                k = 1;
            else
            k = 4;
        }
        else{
            if (y>0)
                k = 2;
            else
                k = 3;
        }
    }
    printf("k = %d", k);
    return 0;
}
