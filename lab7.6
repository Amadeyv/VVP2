#include <stdio.h>
#include <math.h>

int main() {
    float a1, b1, c1, a2, b2, c2, x, y;
    printf("koeficient a1 = ");
    scanf("%f", &a1);
    printf("koeficient b1 = ");
    scanf("%f", &b1);
    printf("koeficient c1 = ");
    scanf("%f", &c1);
    printf("koeficient a2 = ");
    scanf("%f", &a2);
    printf("koeficient b2 = ");
    scanf("%f", &b2);
    printf("koeficient c2 = ");
    scanf("%f", &c2);
    if ((a1>0||a1<0)&&(a2>0||a2<0)&&((a1*b2 - a2*b1)<0||(a1*b2 - a2*b1)>0)) {
        y = (a1*c2 - a2*c1)/(a1*b2 - a2*b1);
        printf("y = %f", y);
        x = (c1 - b1*y)/a1;
        printf("x = %f", x);
    }
    else {
        printf("net resheniya");
    }
    return(0);
}
