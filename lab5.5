#include <stdio.h>
#include <math.h>

int main() {
    int  x1, y1, x2, y2, x3, y3;
    float a, b, c, pr, pl, p;
    printf("vvedite koordinati x1 = ");
    scanf("%d", &x1);
    printf("y1 = ");
    scanf("%d", &y1);
    printf("x2 = ");
    scanf("%d", &x2);
    printf("y2 = ");
    scanf("%d", &y2);
    printf("x3 = ");
    scanf("%d", &x3);
    printf("y3 = ");
    scanf("%d", &y3);
    a = sqrt((x2 - x1)*(x2 - x1) + (y2 - y1)*(y2 - y1));
    b = sqrt((x2 - x3)*(x2 - x3) + (y2 - y3)*(y2 - y3));
    c = sqrt((x3 - x1)*(x3 - x1) + (y3 - y1)*(y3 - y1));
    if ((a+b)>c&&(a+c)>b&&(b+c)>a) {
        pr = a + b + c;
        p = pr/2;
        pl = sqrt(p*(p - a)*(p - b)*(p - c));
        printf("perimetr pr = %f\n", pr);
        printf("ploshad pl = %f", pl);
    }
    else {
        printf("takogo treugolnika ne sushestvuet");
    }
    return 0;
}
