#include <stdio.h>
#include <math.h>

int main() {
    float a, p = 3.14, r;
    printf("vvedite ugol(0<a<360) a = ");
    scanf("%f", &a);
    if (a>0&&a<360) {
        r = (a/180)*p;
        printf("r = %f", r);
    }
    else {
        printf("ne sootvetstvuet usloviyam");
    }
    return(0);
}
