#include <stdio.h>
#include <math.h>

int main() {
    int s, n, k;
    printf("n>1\n");
    printf("vvedite chislo n = ");
    scanf("%d", &n);
    k = 1;
    s = 0;
    while (s<n) {
        s = s + k;
        k = k + 1;
    }
    printf("k = %d\n", k);
    printf("s = %d", s);
    return 0;
}
